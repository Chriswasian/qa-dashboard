# QA Results Dashboard

A Python tool that reads pytest test results and generates an HTML dashboard.

## Features
- Reads test results from CSV
- Shows pass/fail stats with color coding

- Displays pass rate percentage
- Auto-generates a dashboard.html file

## How to run
python3 generate.py